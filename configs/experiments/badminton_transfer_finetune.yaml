data:
  processed_path: "data/processed/badminton"
  num_classes: 4  # 事件分類：Smash, Net Play, Rally, Serve
  use_event_labels: true  # 使用事件標籤
  sport: "badminton"

model:
  backbone: "resnet50"
  hidden_size: 512
  num_lstm_layers: 3
  dropout: 0.3
  seq_length: 16
  use_optical_flow: false  # RGB only 版本
  use_pretrained: true  # Backbone 使用 ImageNet 預訓練

training:
  batch_size: 16  # 羽球資料可能較少，使用較小 batch size
  epochs: 50  # 微調全模型需要更多 epochs
  learning_rate: 0.00001  # 🔑 非常小的學習率，避免破壞預訓練權重
  num_workers: 4
  freeze_backbone: false  # 🔑 不凍結，允許微調整個模型
  pretrained_weights: "weights/experiments/tennis_4event_baseline_20251126_072103/best_model.pth"  # 🔑 網球模型權重

notes: "Transfer learning from tennis (4-class events) - Strategy B: Full model fine-tuning"
